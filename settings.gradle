rootProject.name = 'toy-commerce-platform'

include(
    ':app:commerce-api',
    ':app:config-server',
    ':common:common-core',
    ':common:common-web',
    ':domain:member',
    ':domain:catalog',
    ':domain:order',
    ':domain:inventory',
)
